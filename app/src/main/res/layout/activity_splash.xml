<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:id="@+id/splash_main_relative"
    android:layout_height="match_parent" tools:context=".SplashActivity"
    android:background="@color/bg_color"
    android:fitsSystemWindows="true"
    android:theme="@android:style/Theme.DeviceDefault.NoActionBar">

    <!--<TextView android:text="SALES PAD"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/splash_imageview"
        android:padding="10dp"
        android:textSize="25sp"/>-->

    <ImageView
        android:id="@+id/splash_imageview"
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:scaleType="centerInside"
        android:layout_marginLeft="150dp"
        android:layout_centerInParent="true"
        android:src="@drawable/edna_big_new"/>

</RelativeLayout>
